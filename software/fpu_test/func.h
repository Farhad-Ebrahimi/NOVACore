#include <stdint.h>

#ifndef fputetst_H
#define fputetst_H

float mul(float in1, float in2);
void reverse(char* str, int len);
int int_to_str(int32_t x, char str[], int d);  
void fp2str(float value, char *str, int precision);

#endif